import 'package:http/http.dart' as http;
import 'dart:convert';

class PreAPI
{
  static String URL = 'https://localhost:44308/weatherforecast';

  static Future FetchWeatherForeCast()
  {
    var request = http.get(URL);

    if(request.)
  }
}

